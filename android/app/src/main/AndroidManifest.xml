<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      DriveBook is fully offline in Phase 1: all ledger data stays in a
      private, per-install SQLite database with no network access. The
      INTERNET permission is intentionally NOT requested here — it will
      be added only when Google Drive backup ships in a later phase, and
      even then it's used solely to upload/download that one signed-in
      user's own backup file.
    -->
    <application
        android:label="DriveBook"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
